# Express server
PORT=5000

# SMTP settings (optional). If not set, contact messages will be logged to the server console.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_SECURE=false

# Email addresses
CONTACT_FROM="Shadow Interior <no-reply@shadowinterior.example>"
CONTACT_TO=hello@shadowinterior.example
